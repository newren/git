X stash doesn't work right
/ `git sparse-checkout reapply` gives suboptimal message on already-present
  file (it correctly removes the skip-worktree bit, though)
X ./sparsify --undo; is it dropping git sparse-checkout disable's output?
  NO, false alarm
/ 'git reset --hard' claims it won't remove a file but it does:
     $ git reset --hard
     error: Path 'addme' not uptodate; will not remove from working tree.
     HEAD is now at 49eda28 Bigger check
     [newren@tiger stupid (fuller|SPARSE)]$ git ls-files -t
     H b/counting
     S greetings
     S removeme
     [newren@tiger stupid (fuller|SPARSE)]$ ls
     b
/ 'git reset --hard' also won't (clear/set?) SKIP_WORKTREE bit on unchanged
  file that 'git sparse-checkout reapply' will remove
* 'git sparse-checkout reapply' doesn't work nicely with locally removed files



* 'git checkout HEAD <path>' reports "pathspec '<path>' did not match any
  file(s) known to git", which is a lie (maybe add, "did you mean to specify
  --dense?")
* 'git checkout-index <path>' will copy path to working tree, but not clear the
  SKIP_WORKTREE bit.  I bet its --all flag ignores sparsity paths too.


Categorization: (got until low-level commands)
  * already done...at least in part
    * status
    * read-tree
    * update-index (except perhaps when paths specified?)
    * switch, checkout (when given a branch w/o paths)
    * merge, rebase, cherry-pick, revert (they can unsparsify for conflicts)
    * stash (does similarly, based on merge) ... but misses non-conflict case
    * reset --hard (if you have local file but it's skip_worktree, it's skipped)
    * diff (when no revision specified)
    * add (naturally)
    * clean (ignores dirty files that are skip_worktree)
    
  * unaffected
    * branch
    * clean (working dir already clean)
    * clone (well, kinda...)
    * describe
    * fetch
    * gc
    * init
    * maintenance
    * notes
    * pull (but see merge & rebase)
    * push
    * status (since it already ignores SKIP_WORKTREE paths)
    * read-tree (since it already ignores SKIP_WORKTREE paths)
    * update-index (since it already handles SKIP_WORKTREE paths)
    * submodule
    * switch
    * tag

    * config
    * filter-branch (works in separate checkout without sparsity paths anyway)
    * pack-refs
    * prune
    * remote
    * repack
    * replace

    * bugreport
    * count-objects
    * fsck (promisor handling is as close as it gets)
    * gitweb
    * help
    * instaweb
    * merge-tree
    * rerere
    * verify-commit
    * verify-tag

    * <Everything under interacting with others>
    * <Everything under low-level syncing>
    * <Everything under low-level internal helpers>

    * commit-graph
    * hash-object
    * index-pack
    * merge-file
    * merge-index
    * mktag
    * mktree
    * multi-pack-index
    * pack-objects
    * prune-packed
    * symbolic-ref
    * unpack-objects
    * update-ref
    * write-tree

    * diff-files
    * for-each-ref
    * get-tar-commit-id
    * ls-remote
    * merge-base
    * name-rev
    * pack-redundant
    * rev-parse
    * show-index
    * show-ref
    * unpack-file
    * var
    * verify-pack
    
  * default to full-tree
    * archive
    * bundle
    * commit
    * format-patch
    * fast-export
    * fast-import
    * reflog (?)
    * commit-tree
    * ls-files
  * read only, limit to sparsity paths
    * bisect
    * diff
    * gitk
    * grep
    * log
    * shortlog
    * show (?)

    * annotate
    * blame
    * difftool (does this just piggy-back on diff?)
    * show-branch
    * whatchanged (?)

    * cherry
    * diff-index [?]
    * diff-tree
    * ls-tree [?]
    * rev-list [?]
    * 
  * only affected because bugs
    * reset --hard
  * write affected, limit to sparsity paths [similar to status & read-tree]
    * add (already correctly handled)
    * rm
    * mv (does not support globs -- maybe unaffected?)
    * checkout (with or without revisions, with or without glob/dir pathspecs)
    * restore (with or without revisions, with or without glob/dir pathspecs)
    * checkout-index (think globs)
  * write affected, extend sparsity paths as we go
    * merge
    * rebase
    * cherry-pick
    * revert
    * stash [needs fixes]
    * apply
    * am
  * special
    * worktree [copy default sparsity paths]
  * unsure
    * range-diff (like log, or more like format-patch?)


Danny - 1st
Paul - 2nd
Killian - secretary
